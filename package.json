{
  "name": "home-bell",
  "version": "1.0.0",
  "description": "Home Bell Service Marketplace",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install-server": "cd server && npm install",
    "start": "cd server && node server.js"
  }
}
